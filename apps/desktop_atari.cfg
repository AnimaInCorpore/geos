MEMORY {
    ZP:     start = $0000, size = $0100, type = rw, define = yes;
    RAM:    start = $0400, size = $1C00, type = rw, define = yes;
}

SEGMENTS {
    CODE:   load = RAM, type = rw, start = $0400;
    BSS:    load = RAM, type = bss;
}
