# Third-Party Apps

This directory contains external emulator/tools used for Atari XL bring-up and testing.

## Entries

- `A8E/`: bundled as a git submodule (`jsA8E` smoke-testing path and native A8E build).
- `Altirra/`: local install location and setup notes for Altirra sign-off testing.
