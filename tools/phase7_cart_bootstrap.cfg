MEMORY {
	CARTROM: start = $A000, size = $1FFA, fill = yes, file = %O;
	CARTHDR: start = $BFFA, size = $0006, fill = yes, file = %O;
}

SEGMENTS {
	cart:   load = CARTROM, type = ro;
	header: load = CARTHDR, type = ro;
}
